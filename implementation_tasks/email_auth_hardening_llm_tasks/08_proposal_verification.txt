TASK 08: Proposal Verification

Repo root:
/Users/jonathanleung/Documents/C++/debate_system

Goal:
Verify whether the debate proposal flow supports email-mode submission. If it
does not, document this explicitly in the security policy and skip any proposal
email auth changes.

Why this matters:
The original plan left Phase 8 as an open research question ("Verify whether...").
An LLM needs a deterministic answer, not a research task.

Files to read before editing:
- frontend/static/js/propose.js
- backend/routes/proposal_bp.py

Write scope:
- docs/security/auth.md (append one sentence)

Implementation steps:

1. Read frontend/static/js/propose.js
   Search for:
   - `mailto`
   - `buildMailtoLink`
   - `buildEmailBody`
   - `email`
   - `DataBridge`

   Expected finding: propose.js only calls `BDA.api('/api/debate-proposals', {
   method: 'POST', ... })`. There is no email generation logic.

2. Read backend/routes/proposal_bp.py
   Search for:
   - `email`
   - `mailto`
   - `submission`

   Expected finding: only POST /api/debate-proposals and GET
   /api/debate-proposals/mine exist. There is no email-mode endpoint.

3. Document the finding
   Append to docs/security/auth.md under the "Email-mode Submissions" section:

     ## Debate Proposals
     - Debate proposals are submitted exclusively via the authenticated API.
     - There is no email-mode proposal submission. Proposals follow the same
       login-gated flow as direct API posts.

4. Skip implementation
   No code changes are needed for proposals. The existing @login_required on
   POST /api/debate-proposals and the existing Auth gate in propose.js are
   sufficient.

Acceptance criteria:
- A human reading docs/security/auth.md understands that proposals are API-only.
- No new endpoints or frontend changes are made for proposal email mode.
- The finding is based on actual code review, not assumption.

Guardrails:
- If you DO find email-mode proposal code, stop and report it. Do not proceed
  with skipping.
- Do not add email-mode proposal support unless explicitly directed.
