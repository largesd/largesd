================================================================================
LSD FactCheck v1.5.1 — REVISED IMPLEMENTATION GAPS & REMEDIATION PLANS
================================================================================
Document Version: 2.0
Date: 2026-05-03
Status: Post-Audit Revision — Reflects Recent Codebase Changes

================================================================================
TABLE OF CONTENTS
================================================================================
1. Executive Summary
2. Closed Gaps (Implemented Since v1.0 Plan)
3. Remaining Gap 1: Entity Linking (Stub → Full Implementation)
4. Remaining Gap 2: Parallel Evidence Router
5. Remaining Gap 3: Connector Fallback Chains
6. Remaining Gap 4: Policy-Driven Connector Filtering
7. Remaining Gap 5: LLM Decomposition Wiring
8. Remaining Gap 6: WebRAGConnector v1.5 Migration
9. Minor Deviation: Tier 2 Confidence Base
10. Implementation Priority & Sequencing
11. Acceptance Criteria per Gap
12. File Inventory & Module Map

================================================================================
1. EXECUTIVE SUMMARY
================================================================================
Since the initial gap analysis (v1.0 of this plan), substantial progress has
been made on the LSD FactCheck v1.5.1 pipeline. Several previously-identified
gaps are now CLOSED:

  ✅ Tier 3 Promotion Registry        (source_registry.py)
  ✅ Web Evidence Archiving           (normalizer.py — archive.org + fallback)
  ✅ Normative Claim Routing          (decomposition.py — _is_normative())
  ✅ Predictive Claim Handling        (synthesis.py — Rule J)
  ✅ Circuit Breakers                 (rate_limiter.py)
  ✅ Production Audit Trail           (decomposition.py — decompose_synthesize_and_audit())
  ✅ v1.5 Skill Bridge                (v15_skill.py — fully wired)

This revised plan focuses on the REMAINING gaps and updates implementation
details to reflect the CURRENT codebase state (as of 2026-05-03).

================================================================================
2. CLOSED GAPS (IMPLEMENTED SINCE V1.0 PLAN)
================================================================================

  GAP CLOSED — Tier 3 Promotion Registry
  -----------------------------------------------------------------------------
  File: skills/fact_checking/source_registry.py
  
  Status: FULLY IMPLEMENTED
  
  Implementation:
    - SourceReputationRegistry with ReputationEntry dataclass
    - Domain-indexed allowlist for Tier 3 → Tier 2 promotion
    - Methods: is_promoted(), get_promoted_tier(), promote()
  
  Integration needed:
    - Wire into V15FactCheckingSkill._retrieve_evidence() so that retrieved
      Tier 3 evidence is passed through the registry before synthesis.
    - Current state: registry exists but is NOT invoked in the skill.

  GAP CLOSED — Web Evidence Archiving
  -----------------------------------------------------------------------------
  File: skills/fact_checking/normalizer.py
  
  Status: FULLY IMPLEMENTED
  
  Implementation:
    - archive_web_evidence(source_url, page_text) attempts archive.org submission
    - _request_archive_org() POSTs to https://web.archive.org/save/{url}
    - Fallback to snapshot storage key on archive.org failure
    - Returns dict with archive_org_url and snapshot_storage_key
  
  Integration needed:
    - Called by normalizer but NOT automatically invoked during
      V15FactCheckingSkill._retrieve_evidence(). Should be wired so that
      every web-sourced EvidenceItem gets archived before synthesis.

  GAP CLOSED — Normative Claim Routing
  -----------------------------------------------------------------------------
  File: skills/fact_checking/decomposition.py
  
  Status: FULLY IMPLEMENTED
  
  Implementation:
    - _is_normative() detects 14 normative indicators ("should", "ought to",
      "must", "need to", "important to", "better to", "worse to", "right to",
      "wrong to", "immoral", "unethical", "unjust", "morally", "ethically")
    - Decomposer.decompose() routes normative claims to INSUFFICIENT with
      insufficiency_reason="normative_claim_routed_out" before decomposition
    - ValidationResult flag includes "normative_routed": True

  GAP CLOSED — Predictive Claim Handling
  -----------------------------------------------------------------------------
  File: skills/fact_checking/synthesis.py
  
  Status: FULLY IMPLEMENTED
  
  Implementation:
    - SynthesisEngine.synthesize() accepts predictive_subclaim_ids: Set[str]
    - Rule J (predictive rule) marks predictive subclaims as:
        status = UNSUPPORTED
        p = 0.0
        insufficiency_reason = "predictive_claim_not_checkable"
        status_rule_applied = "rule_j_predictive"
  
  Integration needed:
    - V15FactCheckingSkill does not currently SET predictive_subclaim_ids
      before calling synthesis. A claim classifier (or heuristic in the skill)
      should identify predictive claims and pass their subclaim IDs.

  GAP CLOSED — Circuit Breakers
  -----------------------------------------------------------------------------
  File: skills/fact_checking/rate_limiter.py
  
  Status: FULLY IMPLEMENTED
  
  Implementation:
    - CircuitBreaker class with OPEN/CLOSED/HALF_OPEN states
    - Configurable failure_threshold and recovery_timeout
    - RateLimiterManager maintains circuit breakers per source_id
    - Integration with v1 connector layer (models.py CircuitBreakerConfig)

  Integration needed:
    - Circuit breakers exist in the v1 layer but are NOT used in the v1.5
      V15FactCheckingSkill._retrieve_evidence() loop. Should be wired into
      the connector execution path.

  GAP CLOSED — Production Audit Trail
  -----------------------------------------------------------------------------
  File: skills/fact_checking/decomposition.py
  
  Status: FULLY IMPLEMENTED
  
  Implementation:
    - decompose_synthesize_and_audit() wrapper function exists
    - Accepts audit_store: AuditStore parameter
    - Computes previous_hash from audit_store.get_latest_audit_hash()
    - Creates full AuditRecord with hash chain
    - Stores via audit_store.store(audit_record)
  
  Integration needed:
    - V15FactCheckingSkill currently calls decomposer.decompose() and
      synthesis_engine.synthesize() DIRECTLY — it does NOT use the
      decompose_synthesize_and_audit() wrapper. The skill should be updated
      to use the audited path when enable_audit=True.

================================================================================
3. REMAINING GAP 1: ENTITY LINKING (STUB → FULL IMPLEMENTATION)
================================================================================
3.1 SPEC REFERENCE
    - 01_DATA_MODELS.md §1 (Claim model)
    - 03_PIPELINE.md §2 (Entity/Concept Linking)

3.2 CURRENT STATE
    - skills/fact_checking/entity_linker.py EXISTS but is a STUB.
    - EntityLink dataclass is defined with fields:
        entity_id, mention_span, canonical_id, canonical_type,
        canonical_label, linking_confidence, ambiguity_flag, ambiguity_candidates
    - EntityLinker.link(subclaim_text) returns [] with a TODO comment.
    - No Wikidata API calls. No NER. No LLM disambiguation.
    - The pipeline accepts entity_failure_subclaim_ids but nothing populates it.

3.3 TARGET STATE
    EntityLinker.link() returns actual EntityLink objects by:
    a) Extracting entity mentions from subclaim text (NER or regex).
    b) Querying Wikidata wbsearchentities for candidate QIDs.
    c) Using an LLM (or heuristic) to disambiguate among candidates.
    d) Returning links with confidence scores and ambiguity flags.
    e) Generating entity_failure_subclaim_ids for unresolvable entities.

3.4 IMPLEMENTATION PLAN

  PHASE 1 — Wikidata Search Integration (1 day)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/entity_linker.py
  
  The WikidataEntityConnector in v15_connectors.py already has search
  capability. Extend EntityLinker to use it:
  
    class EntityLinker:
        def __init__(self,
                     wikidata_connector: Optional[WikidataEntityConnector] = None,
                     llm_client: Optional[Any] = None,
                     confidence_threshold: float = 0.7):
            self.wikidata = wikidata_connector or WikidataEntityConnector()
            self.llm_client = llm_client
            self.confidence_threshold = confidence_threshold
        
        def link(self, subclaim_text: str) -> List[EntityLink]:
            mentions = self._extract_mentions(subclaim_text)
            links = []
            for mention in mentions:
                candidates = self.wikidata.search_candidates(mention)
                if not candidates:
                    continue
                link = self._disambiguate(mention, subclaim_text, candidates)
                links.append(link)
            return links
        
        def _extract_mentions(self, text: str) -> List[str]:
            '''
            Strategy 1 (preferred): spaCy NER (PERSON, ORG, GPE, PRODUCT, EVENT)
            Strategy 2 (fallback): Capitalized phrase regex
            Strategy 3 (minimal): LLM prompt to extract entity spans
            '''
            # Try spaCy if available
            try:
                import spacy
                nlp = spacy.load("en_core_web_sm")
                doc = nlp(text)
                return [ent.text for ent in doc.ents
                        if ent.label_ in ("PERSON", "ORG", "GPE", "PRODUCT",
                                          "EVENT", "WORK_OF_ART", "LAW")]
            except Exception:
                pass
            
            # Fallback: regex for capitalized phrases
            import re
            return re.findall(r"\b[A-Z][a-zA-Z\s\.]+(?:\b[A-Z][a-zA-Z\s\.]+)*\b", text)
        
        def _disambiguate(self, mention: str, context: str,
                          candidates: List[Dict[str, Any]]) -> EntityLink:
            '''
            If only one candidate → high confidence direct link.
            If multiple candidates and llm_client available → LLM disambiguation.
            If multiple candidates and no LLM → pick highest wbsearchentities score,
                flag ambiguity if scores are close (< 0.2 apart).
            '''
            if len(candidates) == 1:
                c = candidates[0]
                return EntityLink(
                    entity_id=f"entity_{hashlib.sha256(mention.encode()).hexdigest()[:12]}",
                    mention_span=mention,
                    canonical_id=c["qid"],
                    canonical_type=c.get("type", "UNKNOWN"),
                    canonical_label=c["label"],
                    linking_confidence=min(c.get("score", 0.9), 0.95),
                    ambiguity_flag=False,
                    ambiguity_candidates=[],
                )
            
            # Sort by score descending
            candidates.sort(key=lambda x: x.get("score", 0), reverse=True)
            top = candidates[0]
            second = candidates[1] if len(candidates) > 1 else None
            
            # If scores are close, flag ambiguity
            if second and (top.get("score", 0) - second.get("score", 0)) < 0.2:
                return EntityLink(
                    entity_id=f"entity_{hashlib.sha256(mention.encode()).hexdigest()[:12]}",
                    mention_span=mention,
                    canonical_id=top["qid"],
                    canonical_type=top.get("type", "UNKNOWN"),
                    canonical_label=top["label"],
                    linking_confidence=0.55,
                    ambiguity_flag=True,
                    ambiguity_candidates=[c["qid"] for c in candidates[:3]],
                )
            
            # Clear winner
            return EntityLink(
                entity_id=f"entity_{hashlib.sha256(mention.encode()).hexdigest()[:12]}",
                mention_span=mention,
                canonical_id=top["qid"],
                canonical_type=top.get("type", "UNKNOWN"),
                canonical_label=top["label"],
                linking_confidence=min(top.get("score", 0.85), 0.9),
                ambiguity_flag=False,
                ambiguity_candidates=[c["qid"] for c in candidates[:3]],
            )
  
  PHASE 2 — LLM Disambiguation (Optional, 0.5 days)
  -----------------------------------------------------------------------------
  If llm_client is provided, enhance _disambiguate():
  
    def _llm_disambiguate(self, mention, context, candidates) -> Tuple[str, float]:
        prompt = f'''Given the claim: "{context}"
The mention "{mention}" could refer to:
{chr(10).join(f"{i+1}. QID={c['qid']}, Label={c['label']}, Description={c.get('description', 'N/A')}" for i, c in enumerate(candidates))}

Which candidate best matches the mention in this context?
Respond with ONLY the QID and a confidence score (0.0–1.0) in JSON:
{{"qid": "Q...", "confidence": 0.92}}'''
        
        response = self.llm_client.complete(prompt)
        # Parse JSON, validate QID is in candidates, return (qid, confidence)
  
  PHASE 3 — Integration with V15FactCheckingSkill (0.5 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/v15_skill.py
  
  Modify _retrieve_evidence() to also collect entity links:
  
    def __init__(self, ...):
        # ... existing init ...
        self._entity_linker = EntityLinker()
    
    def _retrieve_evidence(self, decomposition: PremiseDecomposition) -> List[EvidenceItem]:
        items: List[EvidenceItem] = []
        entity_failure_ids: List[str] = []
        
        for subclaim in decomposition.atomic_subclaims:
            # Entity linking
            links = self._entity_linker.link(subclaim.text)
            if not links:
                entity_failure_ids.append(subclaim.subclaim_id)
            
            # Evidence retrieval (existing sequential loop)
            for connector in self._connectors:
                try:
                    retrieved = connector.retrieve(subclaim)
                    items.extend(retrieved)
                except Exception:
                    pass
        
        # Store entity failures for synthesis
        self._last_entity_failure_ids = entity_failure_ids
        return items
  
  Then pass entity_failure_ids to synthesis:
  
    v15_result = self._synthesis_engine.synthesize(
        decomposition=decomposition,
        evidence_items=evidence_items,
        entity_failure_subclaim_ids=set(getattr(self, '_last_entity_failure_ids', [])),
    )
  
  PHASE 4 — Testing (1 day)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/tests/test_entity_linker.py (NEW)
  
  Tests:
    - test_link_single_candidate_high_confidence
    - test_link_multiple_candidates_ambiguity_flag
    - test_link_no_candidates_empty_result
    - test_link_spacy_ner_extraction (skip if spaCy unavailable)
    - test_link_regex_fallback_extraction
    - test_entity_failure_ids_populated
    - test_integration_with_v15_skill

3.5 ESTIMATED EFFORT: 3 days

================================================================================
4. REMAINING GAP 2: PARALLEL EVIDENCE ROUTER
================================================================================
4.1 SPEC REFERENCE
    - 03_PIPELINE.md §3 (Evidence Router)
    - Required: "Query all applicable connectors in parallel"

4.2 CURRENT STATE
    - V15FactCheckingSkill._retrieve_evidence() (v15_skill.py:456-467):
      
      def _retrieve_evidence(self, decomposition: PremiseDecomposition) -> List[EvidenceItem]:
          items: List[EvidenceItem] = []
          for subclaim in decomposition.atomic_subclaims:
              for connector in self._connectors:
                  try:
                      retrieved = connector.retrieve(subclaim)
                      items.extend(retrieved)
                  except Exception:
                      pass
          return items
  
  This is PURELY SEQUENTIAL. No asyncio, no ThreadPoolExecutor.
  - If one connector hangs for 30s, ALL subsequent connectors wait.
  - No timeout per connector.
  - No circuit breaker integration.

4.3 TARGET STATE
    Parallel execution of connector.retrieve() calls with:
    a) ThreadPoolExecutor for I/O-bound connector calls.
    b) Per-connector timeouts (configurable, default 30s).
    c) Circuit breaker integration (from rate_limiter.py).
    d) Non-fatal failures — one connector failure does not affect others.
    e) Optional: asyncio support for truly async connectors.

4.4 IMPLEMENTATION PLAN

  PHASE 1 — Parallel Retrieval with ThreadPoolExecutor (1 day)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/v15_skill.py
  
  Replace _retrieve_evidence() with a parallel implementation:
  
    from concurrent.futures import ThreadPoolExecutor, as_completed
    from .rate_limiter import RateLimiterManager
    
    class V15FactCheckingSkill:
        def __init__(self, ..., max_connector_workers: int = 4,
                     connector_timeout: float = 30.0):
            # ... existing init ...
            self._max_workers = max_connector_workers
            self._connector_timeout = connector_timeout
            self._rate_limiter = RateLimiterManager()
        
        def _retrieve_evidence(self, decomposition: PremiseDecomposition) -> List[EvidenceItem]:
            '''Collect evidence from all wired connectors in parallel.'''
            items: List[EvidenceItem] = []
            
            # Build list of (subclaim, connector) tasks
            tasks = []
            for subclaim in decomposition.atomic_subclaims:
                for connector in self._connectors:
                    tasks.append((subclaim, connector))
            
            if not tasks:
                return items
            
            # Execute in parallel
            with ThreadPoolExecutor(max_workers=self._max_workers) as executor:
                future_to_task = {
                    executor.submit(self._retrieve_single, subclaim, connector): (subclaim, connector)
                    for subclaim, connector in tasks
                }
                
                for future in as_completed(future_to_task):
                    subclaim, connector = future_to_task[future]
                    try:
                        retrieved = future.result(timeout=self._connector_timeout)
                        items.extend(retrieved)
                    except Exception as e:
                        logger.warning(
                            f"Connector {connector.connector_id} failed for "
                            f"subclaim {subclaim.subclaim_id}: {e}"
                        )
            
            return items
        
        def _retrieve_single(self, subclaim: AtomicSubclaim,
                             connector: BaseEvidenceConnector) -> List[EvidenceItem]:
            '''
            Single connector retrieval with circuit breaker check.
            '''
            # Check circuit breaker
            cb = self._rate_limiter.get_circuit_breaker(connector.connector_id)
            if cb and cb.is_open():
                logger.warning(f"Circuit breaker OPEN for {connector.connector_id}")
                return []
            
            try:
                result = connector.retrieve(subclaim)
                if cb:
                    cb.record_success()
                return result
            except Exception as e:
                if cb:
                    cb.record_failure()
                raise
  
  NOTE: connector.retrieve() must be thread-safe. If connectors share state
  (e.g., a shared requests.Session), either:
    - Make retrieve() create a new session per call, OR
    - Use threading.Lock around session access, OR
    - Document thread-safety requirements in BaseEvidenceConnector.

  PHASE 2 — Circuit Breaker Integration (0.5 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/rate_limiter.py
  
  The CircuitBreaker and RateLimiterManager already exist. Ensure:
    - RateLimiterManager.get_circuit_breaker(source_id) returns the breaker.
    - V15FactCheckingSkill initializes RateLimiterManager with default configs.
    - If a connector fails 5 times in 60 seconds, circuit opens for 60s.
  
  Update config.py to add v1.5-specific circuit breaker defaults:
  
    @dataclass
    class FactCheckConfig:
        # ... existing fields ...
        max_connector_workers: int = 4
        connector_timeout_seconds: float = 30.0
        circuit_breaker_threshold: int = 5
        circuit_breaker_recovery_seconds: float = 60.0

  PHASE 3 — Testing (1 day)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/tests/test_v15_skill.py (extend)
  
  Tests:
    - test_parallel_retrieval_all_connectors_succeed
    - test_parallel_retrieval_one_connector_times_out
    - test_parallel_retrieval_one_connector_fails
    - test_circuit_breaker_opens_after_threshold
    - test_circuit_breaker_closes_after_recovery
    - test_sequential_fallback_when_single_worker

4.5 ESTIMATED EFFORT: 2.5 days

================================================================================
5. REMAINING GAP 3: CONNECTOR FALLBACK CHAINS
================================================================================
5.1 SPEC REFERENCE
    - 03_PIPELINE.md §3 (Evidence Router)
    - Required: "Configurable fallback per connector type"
    - Example: BLS API → Census API → Tier 2 RAG

5.2 CURRENT STATE
    - No fallback chain implementation.
    - Each connector stands alone; failures return empty evidence silently.
    - ConnectorRegistry.default_connectors() returns a flat list.

5.3 TARGET STATE
    When a connector fails (or returns empty), automatically try a configured
    fallback connector. Fallback chains are defined per claim type and
    connector role.

5.4 IMPLEMENTATION PLAN

  PHASE 1 — Fallback Chain Configuration (0.5 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/config.py
  
  Add fallback chain configuration:
  
    FALLBACK_CHAINS: Dict[str, List[str]] = {
        # Primary connector → [fallback 1, fallback 2, ...]
        "bls": ["census_api", "curated_rag"],
        "wikidata": ["curated_rag"],
        "crossref": ["pubmed_api", "curated_rag"],
        "brave_search": ["curated_rag"],
    }
  
  Or, more granularly, per claim type:
  
    CLAIM_TYPE_FALLBACKS: Dict[ClaimType, Dict[str, List[str]]] = {
        ClaimType.NUMERIC_STATISTICAL: {
            "bls": ["census_api", "curated_rag"],
            "census_api": ["curated_rag"],
        },
        ClaimType.SCIENTIFIC: {
            "crossref": ["pubmed_api", "curated_rag"],
        },
    }

  PHASE 2 — Fallback-Aware Retrieval (1 day)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/v15_skill.py
  
  Modify _retrieve_single() to support fallback:
  
    def _retrieve_with_fallback(self, subclaim: AtomicSubclaim,
                                 primary_connector: BaseEvidenceConnector,
                                 claim_type: ClaimType) -> List[EvidenceItem]:
        '''
        Try primary connector. If it fails or returns empty,
        walk the fallback chain.
        '''
        chain = self._get_fallback_chain(primary_connector.connector_id, claim_type)
        connectors_to_try = [primary_connector] + [
            self._get_connector_by_id(cid) for cid in chain
        ]
        
        for connector in connectors_to_try:
            if connector is None:
                continue
            try:
                result = self._retrieve_single(subclaim, connector)
                if result:  # Non-empty result
                    return result
            except Exception:
                continue
        
        return []
    
    def _get_fallback_chain(self, connector_id: str,
                            claim_type: ClaimType) -> List[str]:
        '''Look up fallback chain from config.'''
        # First try claim-type-specific chains
        type_chains = self.config.claim_type_fallbacks.get(claim_type, {})
        if connector_id in type_chains:
            return type_chains[connector_id]
        
        # Then try global chains
        return self.config.fallback_chains.get(connector_id, [])
    
    def _get_connector_by_id(self, connector_id: str) -> Optional[BaseEvidenceConnector]:
        for c in self._connectors:
            if c.connector_id == connector_id:
                return c
        return None
  
  PHASE 3 — ConnectorRegistry Enhancement (0.5 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/v15_connectors.py
  
  Add to ConnectorRegistry:
  
    @classmethod
    def connectors_with_fallbacks(cls, mode: str = "default") -> List[BaseEvidenceConnector]:
        '''
        Return connectors plus any fallback connectors needed by the
        configured fallback chains.
        '''
        primary = cls.default_connectors()
        primary_ids = {c.connector_id for c in primary}
        
        # Add any fallback connectors not already in primary set
        all_needed = set(primary_ids)
        for chain in config.FALLBACK_CHAINS.values():
            all_needed.update(chain)
        
        missing = all_needed - primary_ids
        # Instantiate missing connectors from a known factory map
        # ...
        return primary + additional

  PHASE 4 — Testing (1 day)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/tests/test_v15_skill.py (extend)
  
  Tests:
    - test_fallback_chain_executes_on_failure
    - test_fallback_chain_skipped_on_success
    - test_fallback_chain_exhausted_returns_empty
    - test_fallback_respects_claim_type
    - test_fallback_connector_not_found_graceful

5.5 ESTIMATED EFFORT: 3 days

================================================================================
6. REMAINING GAP 4: POLICY-DRIVEN CONNECTOR FILTERING
================================================================================
6.1 SPEC REFERENCE
    - 03_PIPELINE.md §3 (Evidence Router)
    - Required: "Apply policy filters (source type, temporal constraints)"

6.2 CURRENT STATE
    - EvidencePolicy is loaded via get_default_policy() in v15_skill.py.
    - The policy is NOT used to filter connectors before retrieval.
    - The policy is NOT used to filter evidence after retrieval.
    - Temporal constraints, source type restrictions, and cross-verification
      settings from the policy are ignored during routing.

6.3 TARGET STATE
    The EvidencePolicy actively shapes connector selection and evidence
    acceptance:
    a) Source type whitelist/blacklist filters connectors.
    b) Temporal constraints filter evidence by date.
    c) Cross-verification settings require multiple independent sources.
    d) Minimum tier requirements reject low-tier evidence for high-stakes claims.

6.4 IMPLEMENTATION PLAN

  PHASE 1 — Policy-Aware Connector Selection (0.5 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/v15_skill.py
  
    def _select_connectors_for_claim(self, claim_type: ClaimType,
                                      policy: EvidencePolicy) -> List[BaseEvidenceConnector]:
        '''
        Filter connectors based on policy source_type preferences.
        '''
        if not policy.allowed_source_types:
            return self._connectors
        
        return [
            c for c in self._connectors
            if c.source_type in policy.allowed_source_types
            or c.source_type == "universal"  # Always allowed
        ]
  
  PHASE 2 — Policy-Aware Evidence Filtering (0.5 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/v15_skill.py
  
    def _filter_evidence_by_policy(self, evidence: List[EvidenceItem],
                                    policy: EvidencePolicy) -> List[EvidenceItem]:
        '''
        Apply policy constraints to retrieved evidence.
        '''
        filtered = []
        for ev in evidence:
            # Temporal constraint check
            if policy.temporal_range and ev.evidence_date:
                if not (policy.temporal_range[0] <= ev.evidence_date <= policy.temporal_range[1]):
                    continue
            
            # Source type check
            if policy.allowed_source_types and ev.source_type not in policy.allowed_source_types:
                continue
            
            # Minimum tier check
            if policy.minimum_evidence_tier and ev.tier < policy.minimum_evidence_tier:
                continue
            
            filtered.append(ev)
        
        return filtered
  
  PHASE 3 — Cross-Verification Enforcement (0.5 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/v15_skill.py
  
    def _enforce_cross_verification(self, evidence: List[EvidenceItem],
                                     policy: EvidencePolicy) -> List[EvidenceItem]:
        '''
        If policy.requires_cross_verification=True, ensure at least
        2 independent source groups are represented.
        '''
        if not policy.requires_cross_verification:
            return evidence
        
        groups = set()
        for ev in evidence:
            if ev.source_independence_group_id:
                groups.add(ev.source_independence_group_id)
        
        if len(groups) < 2:
            logger.warning(
                f"Cross-verification failed: only {len(groups)} source group(s) "
                f"found, policy requires 2+"
            )
            # Option A: Return evidence anyway (synthesis will handle)
            # Option B: Return empty (strict mode)
            # Default to A for now.
        
        return evidence
  
  PHASE 4 — Integration (0.5 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/v15_skill.py
  
  Modify check_fact() flow:
  
    def check_fact(self, claim_text: str, ...):
        # ... existing setup ...
        decomposition = self._get_decomposition(claim_text)
        claim_type = _claim_type_from_text(claim_text)
        policy = get_default_policy(claim_type)
        
        # Select connectors based on policy
        active_connectors = self._select_connectors_for_claim(claim_type, policy)
        
        # Retrieve evidence
        evidence_items = self._retrieve_evidence(decomposition, active_connectors)
        
        # Filter by policy
        evidence_items = self._filter_evidence_by_policy(evidence_items, policy)
        evidence_items = self._enforce_cross_verification(evidence_items, policy)
        
        # Synthesize
        v15_result = self._synthesis_engine.synthesize(...)
        # ...
  
  PHASE 5 — Testing (1 day)
  -----------------------------------------------------------------------------
  Tests:
    - test_policy_filters_connectors_by_source_type
    - test_policy_filters_evidence_by_date
    - test_policy_rejects_below_minimum_tier
    - test_cross_verification_warns_on_single_source
    - test_cross_verification_passes_with_multiple_sources

6.5 ESTIMATED EFFORT: 3 days

================================================================================
7. REMAINING GAP 5: LLM DECOMPOSITION WIRING
================================================================================
7.1 SPEC REFERENCE
    - 03_PIPELINE.md §1 (Decomposition)
    - 01_DATA_MODELS.md (PremiseDecomposition)

7.2 CURRENT STATE
    - Decomposer.__init__() accepts an optional llm_backend: Callable parameter.
    - V15FactCheckingSkill._get_decomposition() always instantiates:
        Decomposer()  # No llm_backend passed
    - Therefore, LLM-assisted decomposition is NEVER used in production.
    - The deterministic fallback (simple ATOMIC wrapping) is always used.

7.3 TARGET STATE
    V15FactCheckingSkill optionally wires an LLM backend into Decomposer
    when the claim is complex enough to benefit from structured decomposition.

7.4 IMPLEMENTATION PLAN

  PHASE 1 — LLM Backend Interface (0.5 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/decomposition.py
  
  The Decomposer already accepts llm_backend. Define a standard interface:
  
    from typing import Protocol
    
    class LLMBackend(Protocol):
        def decompose_claim(self, claim_text: str, claim_type: ClaimType) -> Optional[PremiseDecomposition]:
            '''
            Return a PremiseDecomposition or None if the LLM cannot decompose.
            '''
            ...
  
  PHASE 2 — Simple LLM Backend Implementation (1 day)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/llm_backend.py (NEW)
  
    class SimpleLLMBackend:
        def __init__(self, client: Any, model: str = "gpt-4"):
            self.client = client
            self.model = model
        
        def decompose_claim(self, claim_text: str, claim_type: ClaimType) -> Optional[PremiseDecomposition]:
            prompt = f'''Decompose this claim into atomic subclaims.
            
Claim: "{claim_text}"
Claim type: {claim_type.value}

Return a JSON object with:
- subclaims: list of {{"id": "sc_...", "text": "...", "node_type": "ATOMIC|AND|OR"}}
- relationships: how subclaims combine (AND, OR, IF_THEN)

If the claim is already atomic, return a single subclaim.'''
            
            try:
                response = self.client.complete(prompt, model=self.model)
                parsed = json.loads(response)
                return self._parsed_to_decomposition(parsed, claim_text)
            except Exception:
                return None
        
        def _parsed_to_decomposition(self, parsed: Dict, claim_text: str) -> PremiseDecomposition:
            # Convert LLM JSON output to PremiseDecomposition
            # ... validation, provenance span creation, etc.
  
  PHASE 3 — Skill Integration with Feature Flag (0.5 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/v15_skill.py
  
    def __init__(self, ..., llm_backend: Optional[Any] = None,
                 enable_llm_decomposition: bool = False):
        # ... existing init ...
        self._llm_backend = llm_backend
        self._enable_llm_decomposition = enable_llm_decomposition
    
    def _get_decomposition(self, claim_text: str) -> PremiseDecomposition:
        premise = CanonicalPremise(...)
        
        if self._enable_llm_decomposition and self._llm_backend:
            decomposer = Decomposer(llm_backend=self._llm_backend)
        else:
            decomposer = self._decomposer or Decomposer()
        
        return decomposer.decompose(premise)
  
  PHASE 4 — Testing (0.5 days)
  -----------------------------------------------------------------------------
  Tests:
    - test_llm_decomposition_used_when_enabled
    - test_deterministic_fallback_when_llm_disabled
    - test_llm_failure_falls_back_to_deterministic
    - test_decomposition_validation_catches_llm_errors

7.5 ESTIMATED EFFORT: 2.5 days

================================================================================
8. REMAINING GAP 6: WebRAGConnector v1.5 MIGRATION
================================================================================
8.1 SPEC REFERENCE
    - 01_DATA_MODELS.md §7 (Connectors)
    - 03_PIPELINE.md §3 (Connector Layer)

8.2 CURRENT STATE
    - skills/fact_checking/web_rag_connector.py implements the OLD v1 interface:
        - Inherits SourceConnector
        - Returns SourceResult objects
        - Uses SourceConfidence enum
    - The v1.5 connector interface is BaseEvidenceConnector in v15_connectors.py.
    - web_rag_connector.py has NOT been migrated to return EvidenceItem objects.
    - The v1.5 search connector is BraveSearchConnector in v15_connectors.py.

8.3 TARGET STATE
    Either:
    A) Migrate WebRAGConnector to implement BaseEvidenceConnector and return
       EvidenceItem objects, OR
    B) Deprecate WebRAGConnector and consolidate all web search into
       BraveSearchConnector.

8.4 IMPLEMENTATION PLAN (Option A: Migration)

  PHASE 1 — Interface Adaptation (0.5 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/web_rag_connector.py
  
    from .v15_connectors import BaseEvidenceConnector
    from .v15_models import EvidenceItem, VerdictScope, Direction
    
    class WebRAGConnector(BaseEvidenceConnector):
        def __init__(self, ...):
            # ... existing init ...
        
        @property
        def connector_id(self) -> str:
            return "web_rag"
        
        @property
        def connector_version(self) -> str:
            return "2.0.0"
        
        def retrieve(self, subclaim: AtomicSubclaim) -> List[EvidenceItem]:
            # Call existing search logic
            old_results = self._search_legacy(subclaim.text)
            
            # Map SourceResult → EvidenceItem
            return [self._to_evidence_item(r, subclaim) for r in old_results]
        
        def _to_evidence_item(self, source_result: SourceResult,
                              subclaim: AtomicSubclaim) -> EvidenceItem:
            return EvidenceItem(
                evidence_id=f"ev_{uuid.uuid4().hex[:16]}",
                subclaim_id=subclaim.subclaim_id,
                source_url=source_result.url,
                source_title=source_result.title,
                source_type="web",
                tier=3,  # Web RAG is Tier 3 unless promoted
                quote_or_span=source_result.snippet,
                quote_context=source_result.content[:500],
                verdict_scope=VerdictScope.PARTIAL,
                direction=Direction.SUPPORTS if source_result.relevance > 0.5 else Direction.IRRELEVANT,
                relevance_score=source_result.relevance,
                raw_response_hash=hashlib.sha256(
                    (source_result.snippet + source_result.content).encode()
                ).hexdigest(),
                source_independence_group_id=f"web_rag:{urlparse(source_result.url).netloc}",
                llm_direction_allowed=False,
            )
  
  PHASE 2 — Deprecation Path (0.25 days)
  -----------------------------------------------------------------------------
  If Option B (deprecation) is preferred:
    - Mark WebRAGConnector as @deprecated in docstring.
    - Add DeprecationWarning on __init__.
    - Update ConnectorRegistry.default_connectors() to use BraveSearchConnector
      instead of WebRAGConnector.
    - Remove WebRAGConnector from default connector lists in v15_skill.py.

  PHASE 3 — Testing (0.25 days)
  -----------------------------------------------------------------------------
  Tests:
    - test_web_rag_returns_evidence_items
    - test_web_rag_evidence_has_correct_tier
    - test_web_rag_source_independence_group_set

8.5 ESTIMATED EFFORT: 1 day (Option A) or 0.5 days (Option B)

================================================================================
9. MINOR DEVIATION: TIER 2 CONFIDENCE BASE
================================================================================
9.1 SPEC REFERENCE
    - 02_SYNTHESIS_ENGINE.md (Confidence calculation)
    - Spec says: Tier 2 confidence base = 0.8

9.2 CURRENT STATE
    - skills/fact_checking/synthesis.py uses 0.85 (confirmed via grep).

9.3 FIX (0.25 days)
  -----------------------------------------------------------------------------
  File: skills/fact_checking/synthesis.py
  
  Change TIER_2_CONFIDENCE_BASE from 0.85 to 0.80.
  Update any test assertions that depend on 0.85.
  Run full test suite: pytest skills/fact_checking/tests/ -v

9.4 ESTIMATED EFFORT: 0.25 days

================================================================================
10. IMPLEMENTATION PRIORITY & SEQUENCING
================================================================================
Recommended order based on impact and dependencies:

  SPRINT 1 — Performance & Reliability (Week 1)
  -----------------------------------------------------------------------------
  P1a. Parallel Evidence Router              (2.5d) — Biggest perf win
  P1b. Policy-Driven Connector Filtering     (3d)   — Required for correctness
  P1c. Circuit Breaker Wiring                (0.5d) — Reliability, depends on P1a
  
  SPRINT 2 — Completeness (Week 2)
  -----------------------------------------------------------------------------
  P2a. Connector Fallback Chains             (3d)   — Robustness
  P2b. Entity Linking (stub → full)          (3d)   — Spec compliance
  P2c. Tier 2 Confidence Base Fix            (0.25d)
  
  SPRINT 3 — Advanced Features (Week 3)
  -----------------------------------------------------------------------------
  P3a. LLM Decomposition Wiring              (2.5d) — Quality of decomposition
  P3b. WebRAGConnector Migration/Deprecation (1d)   — Tech debt
  P3c. Audit Trail Integration in Skill      (1d)   — Use decompose_synthesize_and_audit()
  
  SPRINT 4 — Integration & Hardening (Week 4)
  -----------------------------------------------------------------------------
  P4a. End-to-end tests with all gaps closed
  P4b. Performance benchmarking
  P4c. Documentation updates

================================================================================
11. ACCEPTANCE CRITERIA PER GAP
================================================================================

  Gap 1 — Entity Linking
  -----------------------------------------------------------------------------
  [ ] EntityLinker.link() returns non-empty results for claims with named entities
  [ ] At least one of: spaCy NER, regex, or LLM extraction is active
  [ ] Wikidata API queried for candidate QIDs
  [ ] Ambiguous entities flagged with ambiguity_flag=True
  [ ] EntityLink objects include canonical_id (QID), canonical_label, confidence
  [ ] Unresolvable entities contribute to entity_failure_subclaim_ids
  [ ] Integration test: claim → decomposition → entity links → synthesis

  Gap 2 — Parallel Evidence Router
  -----------------------------------------------------------------------------
  [ ] _retrieve_evidence() uses ThreadPoolExecutor or asyncio.gather
  [ ] Per-connector timeout configurable (default 30s)
  [ ] One connector timeout does not block others
  [ ] Circuit breaker state checked before each connector call
  [ ] Circuit breaker records success/failure per connector
  [ ] p95 latency for 4 connectors < 5 seconds (vs current sequential ~30s+)
  [ ] All existing tests still pass

  Gap 3 — Connector Fallback Chains
  -----------------------------------------------------------------------------
  [ ] Configurable fallback chains per connector ID and claim type
  [ ] Fallback executes automatically when primary fails or returns empty
  [ ] Fallback chain walked in order until non-empty result or exhaustion
  [ ] Missing fallback connectors handled gracefully (no crash)
  [ ] Audit trail records which connector ultimately provided evidence

  Gap 4 — Policy-Driven Connector Filtering
  -----------------------------------------------------------------------------
  [ ] EvidencePolicy filters connector selection before retrieval
  [ ] EvidencePolicy filters evidence after retrieval (temporal, tier, source type)
  [ ] Cross-verification warning logged when < 2 source groups
  [ ] Policy respects claim_type-specific defaults
  [ ] Minimum evidence tier enforced

  Gap 5 — LLM Decomposition Wiring
  -----------------------------------------------------------------------------
  [ ] V15FactCheckingSkill accepts llm_backend parameter
  [ ] Feature flag enable_llm_decomposition controls usage
  [ ] When enabled, complex claims use LLM decomposition
  [ ] When disabled or LLM fails, deterministic fallback activates
  [ ] LLM decompositions pass structural validation (depth ≤ 3, reachable ATOMICs)

  Gap 6 — WebRAGConnector v1.5 Migration
  -----------------------------------------------------------------------------
  [ ] WebRAGConnector implements BaseEvidenceConnector, OR
  [ ] WebRAGConnector deprecated and removed from defaults
  [ ] All connector returns are EvidenceItem objects (no SourceResult leakage)
  [ ] Source independence group IDs populated

================================================================================
12. FILE INVENTORY & MODULE MAP
================================================================================
NEW FILES TO CREATE
--------------------------------------------------------------------------------
skills/fact_checking/llm_backend.py              # Gap 5: LLM decomposition backend
skills/fact_checking/tests/test_entity_linker.py # Gap 1 tests
skills/fact_checking/tests/test_evidence_router.py # Gap 2+3+4 tests (optional)

EXISTING FILES TO MODIFY
--------------------------------------------------------------------------------
skills/fact_checking/entity_linker.py            # Gap 1: Implement link(), NER, Wikidata
skills/fact_checking/v15_skill.py                # Gap 2,3,4,5,6: Parallel router,
                                                 # fallback chains, policy filters,
                                                 # LLM decomposition wiring
skills/fact_checking/config.py                   # Gap 2,3: Add fallback chains,
                                                 # circuit breaker defaults,
                                                 # parallel worker config
skills/fact_checking/rate_limiter.py             # Gap 2: Ensure circuit breaker
                                                 # accessible by connector ID
skills/fact_checking/synthesis.py                # Minor: Tier 2 base 0.85 → 0.80
skills/fact_checking/web_rag_connector.py        # Gap 6: Migrate or deprecate
skills/fact_checking/decomposition.py            # Gap 5: Define LLMBackend protocol

EXISTING FILES TO VERIFY (Closed Gaps — Ensure Integration)
--------------------------------------------------------------------------------
skills/fact_checking/source_registry.py          # CLOSED: Wire promote() into
                                                 # V15FactCheckingSkill evidence path
skills/fact_checking/normalizer.py               # CLOSED: Ensure archive_web_evidence()
                                                 # called for web EvidenceItems
skills/fact_checking/decomposition.py            # CLOSED: Normative routing — verify
                                                 # active in skill path
skills/fact_checking/synthesis.py                # CLOSED: Predictive Rule J — verify
                                                 # predictive_subclaim_ids populated

================================================================================
APPENDIX A: CLOSED GAP INTEGRATION CHECKLIST
================================================================================
The following gaps are IMPLEMENTED but NOT FULLY WIRED into the production
skill path. Before declaring v1.5.1 complete, verify each:

  [ ] Tier 3 Promotion: SourceReputationRegistry.promote() called in
      V15FactCheckingSkill after evidence retrieval, before synthesis.
  
  [ ] Web Archiving: archive_web_evidence() called for every web-sourced
      EvidenceItem in the normalizer or skill path.
  
  [ ] Normative Routing: _is_normative() check active in the default
      Decomposer.decompose() path (it is — verify skill uses it).
  
  [ ] Predictive Handling: predictive_subclaim_ids passed to synthesis
      when claims contain predictive language (add heuristic or classifier).
  
  [ ] Circuit Breakers: RateLimiterManager circuit breakers checked in
      V15FactCheckingSkill connector loop (currently not wired).
  
  [ ] Audit Trail: V15FactCheckingSkill uses decompose_synthesize_and_audit()
      instead of direct decompose() + synthesize() when enable_audit=True.

================================================================================
APPENDIX B: QUICK WINS (Under 1 Day Each)
================================================================================
  1. Tier 2 Confidence Base Fix                → 0.25d, zero risk
  2. Wire SourceReputationRegistry into skill  → 0.5d, registry exists
  3. Wire archive_web_evidence into skill      → 0.5d, function exists
  4. Wire circuit breakers into skill loop     → 0.5d, CircuitBreaker exists
  5. Switch skill to audited pipeline wrapper  → 0.5d, wrapper exists
  6. Add predictive heuristic to skill         → 0.5d, synthesis already supports

Total quick wins: ~2.75 days → closes 6 integration gaps immediately.

================================================================================
END OF DOCUMENT
================================================================================
