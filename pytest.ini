[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
pythonpath = backend
filterwarnings =
    ignore:ALLOWED_ORIGINS not set; CORS disabled for safety:UserWarning
    ignore:ADMIN_ACCESS_MODE is set to 'authenticated'.*:RuntimeWarning
